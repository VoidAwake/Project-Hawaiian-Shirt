using Hawaiian.Utilities;
using UnityEngine;

public class OST : MonoBehaviour
{
    [SerializeField] AudioSource firstPhase;
    [SerializeField] AudioSource secondPhase;
    [SerializeField] AudioSource thirdPhase;
    [SerializeField] AudioClip[] phases;

    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
